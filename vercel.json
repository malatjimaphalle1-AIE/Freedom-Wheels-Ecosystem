{
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install && npx prisma generate",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/engines",
      "schedule": "0 */6 * * *"
    }
  ]
}
